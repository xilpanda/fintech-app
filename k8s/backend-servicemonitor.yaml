apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: linuxspec-backend
  namespaceSelector:
    matchNames:
      - linuxspec-prod
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
